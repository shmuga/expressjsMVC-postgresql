

/**
 * @namespace
 */
var tls = {};


/**
 * @param {number} port Port number.
 * @param {string=} opt_host Host address.
 * @param {!Object=} opt_options Options object.
 * @param {Function=} opt_callback Callback.
 * @return {!IStream}
 */
tls.connect = function(port, opt_host, opt_options, opt_callback) {};
